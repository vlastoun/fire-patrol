import * as React from 'react';
import PageTemplate from '../PageTemplate';

const SeznamMapa = (props) => {

  return (
    <PageTemplate>
      mapa seznam
    </PageTemplate >
  )
};

export default SeznamMapa;
